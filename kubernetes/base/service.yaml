apiVersion: v1
kind: Service

metadata:
  name: devops-app

spec:
  type: ClusterIP

  selector:
    app: devops-app

  ports:
    - name: http
      protocol: TCP
      port: 5000
      targetPort: 5000
